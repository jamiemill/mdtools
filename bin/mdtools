#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'mdtools'

# todo: parse options and load different class
mdformatter = MDTools::MDFormatter.new(STDIN, STDOUT)
